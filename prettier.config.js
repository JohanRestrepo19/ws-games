/**@type {import("prettier").Config} */
const config = {
    arrowParens: 'avoid',
    bracketSpacing: true,
    printWidth: 80,
    singleQuote: true,
    tabWidth: 4,
    useTabs: false,
};

//eslint-disable-next-line
module.exports = config;
